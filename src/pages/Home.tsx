import React from "react";
import { Form } from "components/Form";
import { Layout } from "@layout/Header";

export const Home = () => {
  return (
    <div>
      <Form />
      <Layout />
    </div>
  );
};
